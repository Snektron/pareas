--- meson.build
+++ meson.build
@@ -143,4 +157,12 @@ executable(
     install: true,
     build_by_default: true,
     include_directories: [include_directories('src'), include_directories('include')]
-)+)
+
+executable(
+    'pareas2c',
+    pareas2c_sources,
+    install: true,
+    build_by_default: true,
+    include_directories: [include_directories('src'), include_directories('include')]
+)
